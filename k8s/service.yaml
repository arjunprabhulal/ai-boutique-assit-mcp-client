apiVersion: v1
kind: Service
metadata:
  name: ai-boutique-mcp-service
  labels:
    app: ai-boutique-mcp
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: ai-boutique-mcp
